# Port where the Socket.IO server will listen
PORT=3000

# Redis connection URL (adjust if using external Redis, password, or TLS)
REDIS_URL=redis://127.0.0.1:6379

# === Instance ID for dynamic Redis consumer groups ===
# In production, ensure each instance has a unique ID to prevent group collisions.
INSTANCE_ID=io-server-1


# Node environment: development | production
NODE_ENV=production